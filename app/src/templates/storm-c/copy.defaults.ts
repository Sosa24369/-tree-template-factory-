/**
 * storm-c — THE HYBRID. COPY IS storm-a's, BYTE-IDENTICAL BY CONSTRUCTION.
 *
 * This file deliberately contains no strings: it re-exports the storm CONTROL's
 * copy object, so the two cannot drift — any edit to storm-a's copy IS an edit
 * to storm-c's, and a diff between them is impossible by definition.
 *
 * HYPOTHESIS (Design Elevation 2026-08-12): the control's proven message
 * (speed-led hero, firm same-day assessment, free documented assessment — all
 * of storm-a's locked copy decisions), delivered in the VARIANT's design
 * direction (storm-b's earth-tone palette family) executed at a premium level,
 * converts better than either parent.
 *
 * The one thing storm-b changes — its insurance-led hero copy — is exactly the
 * thing this hybrid does NOT take. Copy is the control's; only design moves.
 */

export { stormACopy as stormCCopy } from '../storm-a/copy.defaults';
