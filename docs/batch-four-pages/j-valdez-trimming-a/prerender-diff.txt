Prerender diff — built pages vs the live build (e3394aa), asset hashes normalised. 28 of 55 pages change.
Protected pages are the four in protected-routes.json; all four are in the list, each the target of its page.

          demo/summit-tree/agnostic                   64 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          demo/summit-tree/removal-a                 322 changed lines   template-wide CSS/markup as Texas Tree Tops removal-a; demo, no GTM; HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          demo/summit-tree/removal-b                  66 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          demo/summit-tree/removal-c                  66 changed lines   work grid sizes attribute only (criterion 8 fix); Summit has no reviews URL so no button; T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          demo/summit-tree/storm-a                    66 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component) — and, from T5, the corrected stock alts; HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          demo/summit-tree/storm-b                    66 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          demo/summit-tree/storm-c                    66 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          demo/summit-tree/trimming-a                154 changed lines   T5 alt text; HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone) in place of the template's own pill list
          demo/summit-tree/trimming-b                 66 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          demo/summit-tree/trimming-c                 66 changed lines   work grid sizes attribute only (criterion 8 fix, pre-existing); T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          p/j-valdez/agnostic                         60 changed lines   hero-photo-2's focal point (object-position) on its cell; photos pinned to what the page shows live (decisions 28); T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-sweep: focal points on gallery-slide-3 / work-photo-1 (object-position on their cells); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
PROTECTED p/j-valdez/removal-a                       379 changed lines   page 1 target (R1 hero, R2 proof strip, R3 grid, R4 cards, R5/S1 reviews, S2 areas); from T5, hero-proof.2 now carries its record alt; HOLD-sweep: the rail drops its four composites (8 tiles), the proof cell/benefits photos carry focal points, IMG_1122/IMG_1126 re-ingested (new hashes); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          p/j-valdez/removal-a/thank-you               2 changed lines   preload line only: the hero plate is empty, so the logo preload replaces the photo preload
          p/j-valdez/removal-b                        44 changed lines   hero-photo-2's focal point on its two cells; photos pinned to what the page shows live (decisions 28); T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-sweep: gallery-slide-3's focal point on its cell; HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          p/j-valdez/removal-c                        64 changed lines   Google reviews button (a->c copy parity, page 1); hero-photo-2's focal point on its two cells; photos pinned to what the page shows live (decisions 28); T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-sweep: focal points on gallery-slide-3 / work-photo-1; HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
PROTECTED p/j-valdez/trimming-a                      174 changed lines   THE TARGET of page 2 (S3 grid photos + longform, P1/S1 are CSS) and, from page 1, hero-photo-2's focal point on its hero-band cell; HOLD-sweep: gallery-slide-3 and work-photo-1 carry focal points (object-position); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          p/j-valdez/trimming-b                       58 changed lines   hero-photo-2's focal point: object-position on its two cells; nothing else; T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          p/j-valdez/trimming-c                       60 changed lines   hero-photo-2's focal point on its two cells; work grid now declares the slot sizes (criterion 8, pre-existing); T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          p/texas-tree-tops/agnostic                 164 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD: the clip carries a poster attribute; HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
PROTECTED p/texas-tree-tops/removal-a                759 changed lines   THE TARGET of page 3: sixth grid cell (T3), stock-photo and artwork alts (T5); plus the template-wide reviews button (R5), static areas (S2), grid rule (R3), 4:3 cards (R4) from page 1; HOLD-sweep: proof cell 2 / grid slot 3 / card 3 re-pointed to removal work, the rail curated to 11 tiles (the rest deliberately empty), descriptive alts on four restoration photos, gallery-05's focal point, the clip's poster; HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          p/texas-tree-tops/removal-b                150 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD: the clip carries a poster attribute; HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          p/texas-tree-tops/removal-c                152 changed lines   Google reviews button for a->c copy parity; work grid sizes attribute (criterion 8 fix); T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
PROTECTED p/texas-tree-tops/storm-a                  146 changed lines   THE TARGET of page 4: T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); T6 (S1 slider) is CSS; HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          p/texas-tree-tops/storm-b                  146 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          p/texas-tree-tops/storm-c                  146 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          p/texas-tree-tops/trimming-a               324 changed lines   T5 alt text on the three stock photographs; HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone) in place of the template's own pill list
          p/texas-tree-tops/trimming-b               148 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)
          p/texas-tree-tops/trimming-c               148 changed lines   work grid sizes attribute only (criterion 8 fix, pre-existing); T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); HOLD-S2: the areas list is the shared alphabetical grid (computed columns as inline custom properties, cities re-ordered, the marquee markup gone)

CSS-only changes (HTML byte-identical, so absent above): P1 seam + S3 grid rule reach trimming-a x3 (JV PROTECTED, TTT, Summit); the scoped S1 slider reaches removal-a x3 (JV, TTT PROTECTED, Summit) and storm-a x2 (TTT PROTECTED, Summit); the T7 wrapped-chip rule replaces the marquee CSS on the 21 pages the component reaches (their HTML change is listed); HOLD: the call bar's hide-while-a-call-button-is-in-view rule and the footer spacer that reserves the bar's height reach all 27 pages with a bar (CSS + the entry script; no HTML change); HOLD-sweep CSS: storm-a tiles uniform and its handle column, removal-c/trimming-c work cells uniform 3:4, the call-asset line in the footer measure, the areas gap on the cells.
