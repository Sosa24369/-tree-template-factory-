Re-run after decisions 28 (final batch build; the earlier labels for j-valdez agnostic/removal-b/removal-c described a cascade that no longer happens):
Prerender diff — built pages vs the live build (e3394aa), asset hashes normalised. 28 of 55 pages change.
Protected pages are the four in protected-routes.json; all four are in the list, each the target of its page.

          demo/summit-tree/agnostic                   34 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
          demo/summit-tree/removal-a                 226 changed lines   template-wide CSS/markup as Texas Tree Tops removal-a; demo, no GTM
          demo/summit-tree/removal-b                  36 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
          demo/summit-tree/removal-c                  36 changed lines   work grid sizes attribute only (criterion 8 fix); Summit has no reviews URL so no button; T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
          demo/summit-tree/storm-a                    36 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component) — and, from T5, the corrected stock alts
          demo/summit-tree/storm-b                    36 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
          demo/summit-tree/storm-c                    36 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
          demo/summit-tree/trimming-a                  6 changed lines   alt text only: the record alts on the shared stock photographs / slotAlt preferring a record alt (T5)
          demo/summit-tree/trimming-b                 36 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
          demo/summit-tree/trimming-c                 36 changed lines   work grid sizes attribute only (criterion 8 fix, pre-existing); T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
          p/j-valdez/agnostic                         26 changed lines   hero-photo-2's focal point (object-position) on its cell; photos pinned to what the page shows live (decisions 28); T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
PROTECTED p/j-valdez/removal-a                       263 changed lines   page 1 target (R1 hero, R2 proof strip, R3 grid, R4 cards, R5/S1 reviews, S2 areas); from T5, hero-proof.2 now carries its record alt
          p/j-valdez/removal-a/thank-you               2 changed lines   preload line only: the hero plate is empty, so the logo preload replaces the photo preload
          p/j-valdez/removal-b                        28 changed lines   hero-photo-2's focal point on its two cells; photos pinned to what the page shows live (decisions 28); T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
          p/j-valdez/removal-c                        32 changed lines   Google reviews button (a->c copy parity, page 1); hero-photo-2's focal point on its two cells; photos pinned to what the page shows live (decisions 28); T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
PROTECTED p/j-valdez/trimming-a                       26 changed lines   THE TARGET of page 2 (S3 grid photos + longform, P1/S1 are CSS) and, from page 1, hero-photo-2's focal point on its hero-band cell
          p/j-valdez/trimming-b                       28 changed lines   hero-photo-2's focal point: object-position on its two cells; nothing else; T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
          p/j-valdez/trimming-c                       28 changed lines   hero-photo-2's focal point on its two cells; work grid now declares the slot sizes (criterion 8, pre-existing); T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
          p/texas-tree-tops/agnostic                  60 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
PROTECTED p/texas-tree-tops/removal-a                407 changed lines   THE TARGET of page 3: sixth grid cell (T3), stock-photo and artwork alts (T5); plus the template-wide reviews button (R5), static areas (S2), grid rule (R3), 4:3 cards (R4) from page 1
          p/texas-tree-tops/removal-b                 54 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
          p/texas-tree-tops/removal-c                 58 changed lines   Google reviews button for a->c copy parity; work grid sizes attribute (criterion 8 fix); T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
PROTECTED p/texas-tree-tops/storm-a                   54 changed lines   THE TARGET of page 4: T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component); T6 (S1 slider) is CSS
          p/texas-tree-tops/storm-b                   54 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
          p/texas-tree-tops/storm-c                   54 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
          p/texas-tree-tops/trimming-a                 6 changed lines   alt text only: the record alts on the shared stock photographs / slotAlt preferring a record alt (T5)
          p/texas-tree-tops/trimming-b                54 changed lines   T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)
          p/texas-tree-tops/trimming-c                54 changed lines   work grid sizes attribute only (criterion 8 fix, pre-existing); T7: the areas marquee's duplicate aria-hidden list, animated track and tabindex are gone; each city once (shared component)

CSS-only changes (HTML byte-identical, so absent above): P1 seam + S3 grid rule reach trimming-a x3 (JV PROTECTED, TTT, Summit); the scoped S1 slider reaches removal-a x3 (JV, TTT PROTECTED, Summit) and storm-a x2 (TTT PROTECTED, Summit); the T7 wrapped-chip rule replaces the marquee CSS on the 21 pages the component reaches (their HTML change is listed).
