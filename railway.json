{
  "_DEPRECATED": "IGNORED BY RAILWAY AS OF 2026-09-05. Railway replaced Nixpacks with Railpack, and config-as-code (railway.json / railway.toml) is no longer read: this service was deployed with buildCommand, startCommand and healthcheckPath all null, and crash-looped on `Cannot find module '/app/index.js'` \u2014 the root package.json's \"main\". The live configuration is .railway/railway.ts (Infrastructure as Code) plus the build/start scripts in the root package.json. Editing this file has no effect. Kept only as a record of what the settings were.",
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --no-audit --no-fund && npm --prefix app ci --no-audit --no-fund && npm --prefix app run build:dashboard && npm --prefix server ci --no-audit --no-fund"
  },
  "deploy": {
    "startCommand": "node server/index.mjs",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
