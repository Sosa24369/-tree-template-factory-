node_modules/
dist/
.DS_Store

# R3 — nothing secret in the repo. Real values live in .env.local, never committed.
.env
.env.local
.env.*.local

# Raw fetched HTML of the source pages (large, regenerable via scripts/fetch-source.sh).
# The distilled source of truth in source/ IS committed.
source/_raw/

# Vite app
app/node_modules/
app/dist/
node_modules/
app/dist-ssr/
